<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) || " Get some music ♫" %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield(:head) %>
</head>
<body>
  <div id="usernav">
    <% if user_signed_in? %>
      Signed in as <%= current_user.email %>, you have the role of <%= current_user.role %>.
      Not you? <%= link_to "Sign out",  destroy_user_session_path, :method => :delete %>
    <% else %>
      <%= link_to "Sign in", new_user_session_path %> or you can 
      <%= link_to "Sign up",  new_user_registration_path %>
    <% end %>
  </div>
  
  <div id="container">
    <% flash.each do | name, msg| %>
      <%= content_tag :div, msg, :id => "flash_#{name}" %>
    <% end %>
  </div>
  
  <%= yield %>
  
  
  <ul>
  <% User.all.each do |usr| %>
    <li><%= usr.id %></li>
  <% end %>
    
    <%= link_to 'Destroy', user_registration_path, :confirm => 'Are you sure you want to destroy your account?', :method => :delete %>
  </ul>
  
  <%  if current_user.try(:admin?) %>
  <h1>You are an admin!</h1>
  <% else %>
  <h1>Aint no admin anymore</h1>
  <%  end %>
  
</body>
</html>
