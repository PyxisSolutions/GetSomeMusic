<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) || " Get some music ♫" %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield(:head) %>
</head>
<body>
  <div class="container">
    <div>
      <div class="navbar navbar-inverse">
        <div class="navbar-inner"><br/>
          <div class="container">
            <div class="row">
              <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </a>
              <div class="span3">
                <strong><a class="brand" style="font-size:40px;" href="/">♫</a></strong>
              </div>
              <div class="nav-collapse">
                <a href="#">Ahref</a>
                <a href="#">Ahref</a>
                <a href="#">Ahref</a>
              </div>
            </div>
          </div><br/>
        </div>
      </div>
    </div>
    
    <div class="row" id="menu">
      <div class="span3"></div>
      <div class="span6">
        <%= link_to "Home",  "/" %>

        <% if user_signed_in? %>
          <% if current_user.role == "user" %>
            <%= link_to "Dashboard", userdash_index_path %>
          <% else %>
            <%= link_to "Dashboard", banddash_index_path %>
          <% end %>
          <%= link_to "Sign out",  destroy_user_session_path, :method => :delete %><br/>
          Signed in as <%= current_user.email %>, you have the role of <%= current_user.role %>.  
        <% else %>
          <%= link_to "Sign in", new_user_session_path %> or you can 
          <%= link_to "Sign up",  new_user_registration_path %>
        <% end %>
      </div>
    </div>
    
    <div class="row" id="warnings">
      <div class="span3"></div>
      <div class="span6">
        <% if flash.count > 0 %>
        <br/>
        <div class="alert">
          <button type="button" class="close" data-dismiss="alert">×</button>
          <% flash.each do | name, msg| %>
            <%= content_tag :div, msg, :id => "flash_#{name}" %></div> 
          <% end %>
        </div> 
        <% end %>
    </div>
    </div>
    
    <div class="row" >
      <div class="span3"></div>
      <div class="span6 hero-unit">
        <%= yield %>
      </div>
      <div class="span3"></div>
    </div>
    
  </div>
</body>
</html>
